package units;

public class Sniper extends Units{
    public Sniper(int health, int defence, int damage, int actionPoints, String name, int initiave) {
        super(health, defence, 0, damage, actionPoints, name, initiave);
    }
    
}
