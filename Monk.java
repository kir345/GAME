package units;

public class Monk extends Units{
    public Monk(int health, int defence, int mana, int damage, int actionPoints, String name, int initiave) {
        super(health, defence, mana, damage, actionPoints, name, initiave);
    }
    
}
