package units;

public class Rogue extends Units{
    public Rogue(int health, int defence, int damage, int actionPoints, String name, int initiave){
        super(health, defence, 0, damage, actionPoints, name, initiave);
    }
    
}
