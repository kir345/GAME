package units;

public class Spearman extends Units{
    public Spearman(int health, int defence, int damage, int actionPoints, String name, int initiave) {
        super(health, defence, 0, damage, actionPoints, name, initiave);
    }
    
}
